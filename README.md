sudoku_solver_coffeescript
==========================
Sudoku Solver Written In A TDD Style with Coffeescript and Jasmine-Node.

To run specs:

```
$ jasmine-node spec
```

To hack on the coffeescript:

```
$ coffee -wc
```
